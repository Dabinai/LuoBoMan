<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.dabin.www.luobomvp.MainActivity">

  <FrameLayout
      android:layout_above="@+id/groupmain"
      android:id="@+id/fragment"
      android:layout_width="match_parent"
      android:layout_height="match_parent"/>
  <RadioGroup
      android:id="@+id/groupmain"
      android:background="@color/grop"
      android:layout_alignParentBottom="true"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="horizontal">
    <RadioButton
        android:id="@+id/home_btn"
        style="@style/HomeStyle"
        android:checked="true"
        android:text="首页"
        />
    <RadioButton
        android:id="@+id/kind_btn"
        style="@style/KindStyle"
        android:text="分类"
        />
    <RadioButton
        android:id="@+id/shop_btn"
        android:text="购物车"
        style="@style/ShopStyle"
        />
    <RadioButton
        android:id="@+id/mine_btn"
        android:text="个人"
        style="@style/MineStyle"
        />
  </RadioGroup>

  
</RelativeLayout>
